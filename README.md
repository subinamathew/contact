# Contact Card


# Prerequestis

Install UV

`curl -LsSf https://astral.sh/uv/install.sh | sh`

Add Venv

`python3 -m venv env`


`source env/bin/activate`

Add Google adk

`uv pip install google-adk`